# SILMARIL — minimum runtime dependencies
# All free, all widely available. No paid services anywhere in the stack.

feedparser>=6.0.10          # RSS ingestion (Google News, press releases)
requests>=2.31.0            # HTTP
python-dateutil>=2.8.2      # robust date parsing
yfinance>=0.2.40            # free price data (used for backfill + live prices)

# Optional (used by analytics modules when they land — comment in when needed):
# numpy>=1.26
# pandas>=2.2
