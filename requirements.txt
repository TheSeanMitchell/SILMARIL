# SILMARIL — minimum runtime dependencies
# All free, all widely available. No paid services anywhere in the stack.

feedparser>=6.0.10          # RSS ingestion (Google News, SEC EDGAR)
requests>=2.31.0            # HTTP
python-dateutil>=2.8.2      # robust date parsing
yfinance>=0.2.40            # free price data (Yahoo Finance)
pandas>=2.2.0               # yfinance returns DataFrames
numpy>=1.26.0               # pandas dependency
